/**
 * 클라이언트 기능 플래그.
 *
 * 변경은 여기 한 곳에서만 — 관련 UI/가드/스텝이 자동으로 반응.
 */

/**
 * 부모 영역(/parent/*) 접근을 PIN으로 보호할지 여부.
 *
 * - `true`: 가입 시 4자리 PIN 설정 스텝 + `/parent/*` 진입 시 PIN 모달
 *   (아이가 태블릿 만질 때 부모 영역 보호. 프로덕션 배포 시 값)
 * - `false`: PIN 설정 스텝 skip, 부모 영역 프리 패스
 *   (테스트/개인 사용에서 PIN 잊어버림 등 마찰 제거)
 *
 * DB 컬럼·RPC(`set_pin`/`verify_pin`)는 그대로 유지되므로 재활성화는 이 플래그 한 줄만 바꾸면 됨.
 */
export const PIN_REQUIRED = false;
