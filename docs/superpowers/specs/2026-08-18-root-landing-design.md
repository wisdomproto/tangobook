# 루트(`/`) 소개 페이지 설계 — 2026-08-18

지금 `/` 는 `/library` 로 리다이렉트한다([router/index.tsx:163](../../../packages/client/src/router/index.tsx)).
그 자리에 **플랫폼 소개 페이지**를 놓고, 가입 전 사람이 설치도 가입도 없이 만져볼 수 있는
**활동 표면**을 그 아래 편다.

## 왜

- 실사용자 4명 중 3명이 `/library` 에 들어와 **2~3분 책장만 넘기다 나갔다**. 파닉스는 아무도 안 열었다.
  "들어와서 알아서 써"가 안 통한다는 게 이미 측정돼 있다. → memory `real-usage-baseline-2026-08-13`
- 유아 플랫폼은 브랜딩 없이 안 팔린다. 우리 고객은 대개 **아이 하나 키우는 초보 엄마**다.
- 🔴 우리 유일한 우위는 **「설치 전에 해볼 수 있다」** 다. 토도한글·소중한글은 앱을 깔아야 하고,
  소중한글은 「게임 직접 해보기」 버튼조차 앱스토어로 간다. 그래서 활동 검색어에 서 있는 건
  대기업이 아니라 **개인 블로그**다. → memory `coloring-print-seo-2026-08-17`

## 전제 — 게이팅이 바뀌었다 (2026-08-18, `cd266cad`·`a6e49735`)

🔴 **이걸 모르면 반대로 만든다.** 「미로그인은 무료 11권만」은 **끝난 정책**이다.

| | 미로그인 |
|---|---|
| 동화책 **전 권**(266) | ✅ |
| 파닉스 무료 단원 | ✅ |
| 사이드바 어휘 게임(랜덤 책) | ✅ **일부러 열어둠** |
| 독후활동(그 책 단어 익히기) | 🔒 |
| 학습 리포트 | 🔒 |

`canReadBook` 은 파닉스 단원에만 남았다. 어휘 게임을 열어둔 이유는 커밋 메시지에 있다 —
*"게임이 어떤 건지 랜덤 책으로 보여주고, 「방금 읽은 그 책으로 해보기」가 가입 이유"*.

이 변경으로 **가입 지점이 「읽기 전」에서 「읽고 난 뒤」로 옮겨갔다.** 설계가 이걸 따라야 한다.

## 구조

```
/                    소개
/games               활동 허브 (화면 제목은 「놀아보기」 계열, URL 은 /games)
 ├ /games/coloring   색칠하기       [🖨 인쇄] [🎨 칠하기]
 ├ /games/hidden     숨은그림찾기    [🔍 찾기] [🖨 인쇄]
 └ /games/vocab      어휘 게임       ✅ 이미 있음
/worksheet           활동지 — 게임이 아니라 따로
 ├ /worksheet/hangul   제목 「무료 한글 학습지」
 └ /worksheet/english  제목 「영어 파닉스 학습지」
```

`/games` 는 [GamesHubPage.tsx](../../../packages/client/src/pages/GamesHubPage.tsx) 가 이미 있다(98줄, 카드 2장).
2026-08-09 에 사이드바에서 학습게임 축이 빠지며 고아가 됐다 — 새로 만들지 말고 카드를 갈아끼운다.

🔴 **허브 카드에서 한글/알파벳 블록은 뺀다.** 그 둘은 어휘 게임 안의 4종(블록·따라쓰기·그림짝·그림그리기)
중 하나라, 카드로 또 내놓으면 같은 걸 두 군데서 고르게 된다. 라우트는 남긴다.

## 네비게이션

```
[탱고북]                                로그인   [한 달 무료 시작]
학습하기 · 색칠 도안 · 숨은그림찾기 · 어휘 게임 · 활동지
```

- **허브를 안 거치고 바로 간다.** 허브는 둘러보는 사람용이고, 검색으로 온 사람은 자기 페이지에 직행한다.
- 🔴 **모바일에서 햄버거에 넣지 않는다.** 초보 엄마는 햄버거를 안 눌러본다.
  가로 스크롤 pill 줄로 깔되, 라이브러리 캐러셀과 같은 규칙으로 **오른쪽 칩이 살짝 걸쳐** 보여야
  "옆에 더 있다"가 전달된다.
- 이 네비를 활동 페이지에도 그대로 얹으면 **형제 이동이 해결**된다. 별도 하단 줄 불필요.
- 푸터에 같은 링크를 텍스트로 한 번 더 — 크롤러가 확실히 물게.

### 「학습하기」 → `/library`

🔴 「한글·영어 배우기」가 아니라 **「학습하기」**. 이유는 **다국어**다 — 베트남 아이가 한글을 배우고
한국 아이가 영어를 배우므로, 무엇을 배우는지를 라벨에 박으면 언어마다 어긋난다.

앱 사이드바가 들어가면 3축(동화책·어휘 게임·파닉스)으로 갈라주므로 네비가 그 일을 미리 하지 않는다.

### 로그인 버튼

🔴 지금 헤더 라벨이 **「로그인 / 회원가입」** 한 덩어리다. 그대로 두고 옆에 [무료 시작]을 놓으면
**같은 행동을 두 번** 시킨다 — 프로모 CTA와 로그인 버튼이 겹쳐 게스트 CTA를 걷어낸 것이 이 문제였다.

메인에서는 가른다: **로그인 = 작은 텍스트**(기존 회원) / **[한 달 무료 시작] = 코랄 버튼**(신규).

🔴 **로그인 사용자가 `/` 로 오면 `/library` 로 보낸다.** 매일 쓰는 사람이 매번 소개를 보면 안 된다.

## 검색어 실측 (네이버, 2026-08-17~18)

🔴 **활동마다 수요의 성격이 다르다. 한 규칙으로 뭉뚱그리면 틀린다.**

| | 활동명 자체 | 「도안」 | 「게임」 |
|---|---:|---:|---:|
| 색칠 | 6,350 | **17,390** (`색칠도안`) | 20 (`온라인색칠공부`) |
| 숨은그림찾기 | **10,970** | 5,480 | 1,090 |

- **색칠 = 종이.** 「도안」계 32,810 vs 「온라인·사이트·무료」계 160 = **200배**. → 인쇄 버튼이 먼저.
- **숨은그림 = 화면.** 활동명이 도안의 2배, 게임 수요도 색칠의 54배. → 찾기 버튼이 먼저.
- 손으로 하는 건 종이, 눈으로 하는 건 화면 — 틀린그림찾기게임 1,370 도 같은 방향(세 번째 표본).
  ⚠️ **해석이지 증거는 아니다.** 판정은 같은 방문자에게 두 버튼을 나란히 주고 GA4 클릭으로.

**활동지**

| 키워드 | 월 검색 |
|---|---:|
| 한글공부 | 2,230 |
| 영어파닉스 | 2,020 |
| 한글학습지 | 950 |
| 한글떼기 | 930 |
| 한글워크북 | 510 |
| 한글활동지 | 240 |
| 파닉스학습지 | 90 |
| **파닉스활동지** | **30** |

🔴 「파닉스 활동지」는 **사실상 아무도 안 친다(30)**. 사이트 안 라벨로는 뜻이 분명해 좋지만
**페이지 제목으로 쓰면 안 된다.**

🔴 **한글·영어는 페이지를 나눈다.** 검색어가 아예 다른 낱말이라(한글 학습지 950 vs 영어 파닉스 2,020)
한 페이지에 몰면 제목을 뭘로 달지 못 정한다. 네비 pill 은 하나, 착지점은 둘.

🔴 **pill 이름은 우리 머릿속 낱말이 아니라 실제로 치는 낱말로.** 「숨은그림」 110 vs
「숨은그림찾기」 10,970 · 「색칠공부프린트」 20 vs 「색칠공부도안」 8,980.

⚠️ **「숨은그림찾기」 10,970 의 뜻은 아직 안 갈랐다** — 성인 취미 수요가 섞여 있을 수 있고
「유아숨은그림찾기」가 490뿐인 게 걸린다. SERP 확인이 필요한데 `DATAFORSEO_LOGIN` 미설정이라 보류.

## 활동 페이지 명세

도안 PNG 한 장이 인쇄와 게임 **둘 다** 된다. 추가 자산이 없다.

```
/games/coloring/오리
   [ 도안 크게 ]
   [🖨 A4로 인쇄]  [🎨 칠하기]
```

- 칸 나누기는 도안 픽셀만 보는 flood fill(`@tangobook/shared`)이라 **정답본이 필요 없다.**
  칸별 색은 **원본 삽화에서 읽는다**(`auto-color.mjs --from=<원본>`) — 도안이 원본을 보고 그린
  그림이라 자리가 겹친다. 실측: 오리 노랑·부리 주황·볼 분홍, 여우 주황·주둥이 크림.
- 인쇄는 **인쇄 CSS**(`window.print()`)로. 도안 2,067장에 PDF 를 미리 구우면 R2 에 2,067개를 올려야 한다.
  ⚠️ 활동지는 반대다 — **이미 A4 레이아웃으로 짜인 HTML** 이라 그대로 인쇄하면 된다.
  두 페이지의 인쇄 방식이 다르므로 같은 컴포넌트로 묶지 말 것.

## 다리 — 활동에서 가입까지

```
검색 「무료 색칠공부 도안」
  → /games/coloring
  → 인쇄하고 나가도 괜찮다        ← 검색 의도의 200배가 여기 있다
  → 화면에서 칠한다
  → 다 칠하면 그 낱말이 나오는 동화 한 쪽 (삽화 + 문장 + 나레이션)
  → 「이 책 읽어볼래요?」          ← 가입 없이 읽힌다
  → 다 읽고 「이 책 단어로 놀아볼까?」  🔒
  → 가입
```

마지막 두 단계는 **이미 있다** — `SceneReveal` + `resolveSceneFromWord` + 낱말→쪽 인덱스
(87낱말·716쪽, `features/phonics-learner/data/word-scenes.json`). 색칠·숨은그림 완료에 같은 걸 물린다.

숨은그림은 **책 1권 = 씬 1장**이라 `storybookId` 가 자연히 정해져 있다. 배선할 게 없다.

🔴 **어휘 게임의 「한 판 = 한 책」을 깨지 말 것.** 게임 플레이어가 `storybookId` 하나로
①정답 음원 프리워밍 캐시 키 ②장면 리빌을 정한다. 여러 책 낱말을 섞으면 음원이 정답 순간에야
합성되어 늦고 **장면 리빌이 안 뜬다** = 이 다리가 죽는다. 게임 축으로 고르게 하더라도
**속은 랜덤 책 1권**을 유지한다.

🔴 **활동 넷에 게이트를 달지 않는다.** 로그인도 이메일도. 게이트 하나가 경쟁사의 앱스토어와
종류만 다른 벽이고, 그게 우리가 이길 유일한 축을 죽인다.

## 루트 본문 — `/intro` 를 승격한다

[IntroPage.tsx](../../../packages/client/src/pages/IntroPage.tsx)(1,466줄)가 이미 그 페이지다.
라이브 데모 상자가 **스크린샷이 아니라 진짜 컴포넌트**고 성능도 손봤다(요청 257→70, 히어로 28→1.5초).
새로 만들면 문구가 두 벌이 되어 한쪽만 낡는다.

🔴 **다만 너무 길다.** 375px 실측:

| | 세로 |
|---|---:|
| **탱고북 `/intro`** | **23.6 화면** |
| Epic | 12.1 |
| HOMER | 8.2 |

원인은 글이 아니라 **라이브 데모 상자 14개**다. 세 군데에 흩어져 있다:

| 자리 | 데모 | 화면 |
|---|---:|---:|
| 한글 파닉스 | 9개 | 8.3 |
| 영어 파닉스 | 3개 | 3.1 |
| 동화책 섹션 안(읽어주기·그림짝·낱말쓰기) | 3개 | 3.2 |

합쳐 **14.6화면(62%)**. 나머지 6개 섹션은 다 합쳐 4.1화면이라 오히려 짧다.
동화책 섹션은 5.3화면 중 3.2가 데모고 실제 책 소개(표지 32장 + 설명)는 1.3뿐이다.

### 새 구성 — 라이브는 **하나**, 그 하나가 연결을 보여준다

🔴 **데모를 줄이는 게 아니라 바꾼다.** 지금은 파닉스 따로 동화책 따로라 **연결을 보여주는 데모가 없다** —
그런데 그 연결이 우리 유일한 차별점이고 포지셔닝 문장(「우리는 그 예문이 동화책이다」) 그 자체다.
지금은 그걸 **글로 설명**하고 있다.

```
히어로   핵심 한 문장 + [한 달 무료 시작]
  ↓
1  낱말 → 동화책 라이브 데모                1.5화면   ← 유일한 라이브
     「낱말 하나 맞혀보세요」          [한글] [English]
     → 그림짝 1문제(탭 두 번)
     → 맞히면 그 낱말이 나오는 동화책 한 쪽 (삽화 · 문장 · 읽어주는 소리)
     → 「배운 낱말을 동화책에서 다시 만납니다」  [직접 해보기 →]
2  파닉스 한글 32 · 영어 39단원 — 활동 이미지 격자    1.5
3  동화책 266권 — 표지 32장                          1.3
4  숫자 · 신뢰                                       0.4
5  [한 달 무료 시작]                                  1.5
```

**약 7화면.** HOMER(8.2)보다 짧다.

🔴 **한 문제로 끝나야 한다.** 히어로 바로 아래에서 "게임 한 판 하세요"는 부담이라 대부분 스크롤로
지나간다. 그래서 **그림짝**이다 — 탭 두 번이면 끝난다. 낱말 쓰기는 획을 다 써야 하고 블록은 자모를
조립해야 해서 둘 다 길다.

배선은 이미 있다 — `SceneReveal` + `resolveSceneFromWord` + 낱말→쪽 인덱스(370낱말).

### 한글·영어는 히어로에서 합치고 데모에서 토글한다

🔴 **히어로에서 둘을 각각 말하지 않는다.** 「한글도 하고 영어도 해요」는 웅진스마트올식
(「유아부터 중학까지」) 백화점 포지션이고, 무명 브랜드가 그러면 무엇을 파는지가 안 남는다.
H1 「오늘 배운 **글자**로」는 이미 **언어 중립**이라 둘 다 덮고, 눈썹 「4~7세 한글·영어」와
부제 「71단원」(32+39)이 영어를 건다.

증명은 **데모 안 토글**로 한다 — 세로가 안 늘면서 「영어도 같은 방식」을 말이 아니라 실물로 보여준다.
(우리가 이미 쓰는 패턴: BookDetailPage 언어 토글 · VocabularyStudyPage 5언어 칩.)

- **기본은 한글.** 광고 본진이 한글이고, 영어는 토글 라벨이 보이는 것만으로 「영어도 있구나」가 전달된다.
- 🔴 **[직접 해보기 →] 도 토글을 따라간다.** 영어를 보다 눌렀는데 한글 단원으로 가면 안 된다.

**실측(2026-08-18)** — 영어도 다리가 있다. 인덱스 370낱말 중 영문 283개, 낱말당 평균 8.3쪽.
CVC 표본 14개 중 11개가 걸린다(`bat`·`can`·`pig` 는 없음 — 없으면 낱말만 읽고 넘어가 안 깨진다).
리빌 대상 책 표본 4권은 **en 글·소리 100%**(타르보사우루스 18/18 · 반쪽이 13/13 등)라 영어도 소리가 난다.
⚠️ 4권 표본이다. 데모에 쓸 책은 en `translations[en].ttsUrl` 을 확인하고 고를 것.

🔴 영어 검색 수요는 한글과 거의 같다 — 「영어파닉스」 2,020 vs 「한글공부」 2,230.
히어로만 한글 톤이고 **격자·활동지 페이지에서는 대등하게** 가야 그 2,020 을 받는다.

🔴 **아래를 이미지로 깔아도 되는 이유는 맨 위 하나가 이미 증명하기 때문이다. 순서를 뒤집지 말 것** —
이미지가 위에 오면 첫인상이 스크린샷 사이트가 되고, 그러면 토도한글·소중한글과 똑같아진다.
`/intro` 를 만들 때 벤치마킹 두 번을 거쳐 세운 원칙(**스크린샷 아닌 진짜 컴포넌트를 라이브로**)이 그 이유다.

활동 이미지는 실촬로 찍는다 — `packages/client/scripts/capture-*.mjs` + 공용 `_capture-lib.mjs`.
🔴 `page.screencast` 는 `setViewport({deviceScaleFactor})` 를 **무시한다**. `scale: 3` 은 해상도가
아니라 확대라 360p 확대본이 나온다(PSNR 28.96dB). 창을 **3배 DPI**(`--force-device-scale-factor=3`)로
띄우고 **`SCALE = 1`** 로 찍어야 54.82dB 이 나온다.

🔴 **활동 카드를 첫 화면에 깔지 않는다.** 활동 검색량 65,800 은 `/games/coloring` 이 먹는 숫자지
루트가 먹는 숫자가 아니다 — 「색칠공부 도안」으로 검색한 사람은 그 페이지에 직행하지 루트를 안 거친다.
루트에 오는 사람은 광고·브랜드로 온 사람이고, 그에게 첫 화면으로 색칠을 보여주면
**「색칠 사이트」로 각인**되어 포지셔닝을 스스로 지운다. 활동은 **네비 pill** 로 늘 손 닿는 곳에 두면 충분하다.

### 히어로 문구

```
4~7세 한글·영어

# 오늘 배운 글자로
# 오늘 동화책을 읽어요

파닉스로 소리를 떼고, 그 글자가 나오는 동화책을 그 자리에서 읽습니다.
71단원 · 동화책 266권

[한 달 무료로 시작하기]
```

🔴 **방향은 「연결」이다.** 벤치마크 여섯은 감정이나 안심을 말하지만(HOMER 「Screen Time You Can
Feel Good About」·소중한글 「즐거운 속도로 배우는 한글」), 우리는 **차별점 자체**를 말한다 —
파닉스는 어디서나 배우고 동화책도 어디에나 있지만, **그 둘이 이어진 곳**은 우리뿐이다.
그리고 바로 아래 라이브 데모가 이 문장을 그 자리에서 증명한다. 헤드라인과 데모가 같은 말을 한다.

- 기존 「한글 파닉스를 배우고 스스로 동화책을 읽어요」는 정확하지만 **기능 설명**이라 마음에 안 닿는다.
- 눈썹은 짧게 — H1 이 이미 파닉스·동화책을 다 말하므로 「4~7세 한글·영어」면 된다.
- 🔴 한글 헤드라인은 `break-keep` 필수. 375px 에서 두 줄로 떨어진다.
- 문구 규칙 통과 확인: 무료 헤드라인 ✕ · 마감일 ✕ · 비교표 ✕ · N배 ✕ · 감수 흉내 ✕ · 가격 ✕.
  「무료」는 CTA 에만 둔다.

⚠️ **「읽어요」는 약속이 조금 크다.** 실제로 일어나는 일은 *그 낱말이 나오는 한 쪽*을 만나는 것이고,
통독은 나레이션이 읽어준다. 정확히 가려면 **「오늘 동화책에서 만나요」** — 다리 문구(「배운 낱말을
동화책에서 다시 만납니다」)와도 같아진다. 부모에게 더 센 약속을 할지, 정확할지의 선택이다.
(기존 H1 의 「스스로 읽어요」에 이미 같은 과장이 있었다.)

**히어로에 쓸 재료**: 게이팅 변경으로 **「가입 없이 266권 다 읽힌다」가 최고 미끼**가 됐다.
벤치마크 여섯 곳 중 이걸 할 수 있는 데가 없다 — 전부 설치나 가입이 먼저다.

## SEO 배선

🔴 SPA 라 검색엔진은 자바스크립트를 안 기다린다. `/` 를 소개 페이지로 바꾸면
**세 개를 같이** 고친다 — 고 썼는데, ✅ 실제로 해 보니 **다섯이었다**(2026-08-21).

| | 곳 | 안 하면 |
|---|---|---|
| 1 | 라우터 `Navigate`(`router/index.tsx`) | 앱 안 클릭이 옛 주소로 남는다 |
| 2 | **서버 301**(`app.ts` — `/intro`·`/hangul`·`/english` → `/`) | 크롤러는 **200 + 빈 셸**을 본다 |
| 3 | `useSeo` canonical (`/intro` → `/`) | 같은 내용이 두 주소로 색인된다 |
| 4 | **sitemap** 에서 `/intro` 제거 | GSC 가 「리디렉션이 있는 페이지」로 뺀다 |
| 5 | **prerender** `STATIC_ROUTES` + **`MIN_BY_ROUTE`** | 기준을 안 옮기면 반쪽 HTML 이 통과한다 |

🔴 **`MIN_BY_ROUTE` 를 빠뜨리기 쉽다** — 목록에서 `/intro` 를 지우고 `/` 를 넣는 것까지는
자연스러운데, 「글자 3,000자 이상이어야 통과」라는 기준은 라우트 키에 매달려 있어서 같이
안 옮기면 기본값으로 떨어진다. 히어로만 구워진 HTML 이 조용히 합격한다.

🔴 **로그인한 사람은 `/library` 로**(`RootEntry`) — 단 `loading` 중엔 랜딩을 그린다.
보내 버리면 첫 방문자에게 소개가 한 프레임 스치고 사라지고, 프리렌더 HTML 도 그 사이 덮인다.

🔴 **코드만 고치고 산출물을 다시 안 구우면 조용히 틀린다.** 이 배선으로 두 번 사고가 났다:
번역 없는 `/:lang` 이 200 빈 셸을 돌려줘 GSC 중복 306건, sitemap 을 다시 안 구워 bare 213개를 3주간 제출.
배포 후 **실제 주소를 `curl` 로 열어 글자가 들어 있는지** 눈으로 확인할 것.

## 자산 현황

| | 지금 | 목표 |
|---|---|---|
| 색칠 도안 | 18장(파닉스 라이브) + 작업판 붙이는 중 | 삽화 전체 2,067장 |
| 숨은그림 씬 | **0장** — 저작 탭은 있고 아무도 안 만듦 | 동화책마다 1장 |
| 활동지 | ✅ 한글 120쪽 · 영어 164쪽 | — |
| 어휘 게임 | ✅ | — |
| 틀린그림찾기 | 코드 없음 | 구현 테스트 후 결정 |

작업판 = `/coloring-plan.html`(2,067낱말, 10장 묶음 프롬프트 + 붙여넣기 → R2).
검사기 = `auto-color.mjs`(칸 나누기·새는 곳·못 짚을 칸).

🔴 **숨은그림 씬 0장이 가장 큰 구멍이다.** 검색 축이 18,290 으로 색칠 다음인데 콘텐츠가 없다.
저작 도구(`HiddenObjectEditorTab`)는 이미 있어 씬 이미지만 있으면 핫스팟 찍는 건 빠르다 —
색칠 2,067장보다 훨씬 먼저 끝난다.

## 미결

- 히어로 한 문장
- 활동 이미지 격자에 무엇을 몇 장 넣을지(한글·영어 각각)
- 「숨은그림찾기」 검색 의도 확인(SERP, DataForSEO 미설정)
- 가입한 사용자가 활동에 다시 찾아올 길 — 지금 사이드바 3축이고 `AppShell.test.tsx` 가 가드로 잡는다.
  넷째 축을 넣으면 그 테스트가 깨지고 아이 화면에 축이 하나 는다. **"도안 어디서 봐요"가 실제로
  나올 때 붙인다.**
- 🔜 별건 — **학습자 화면 재설계**(「코스 등록 → 코스 시작」). 지금 화면이 "뭐부터 해야 하지?"라는
  지적이 있었고, 실사용 4명 중 3명의 증상이 정확히 그것이었다. 루트와 섞지 말고 따로 잡는다.

---

## ✅ 실행 결과 (2026-08-21)

루트 승격 + 히어로 재구성 완료. 이 문서가 설계한 「라이브는 하나」가 **그림에도 그대로 적용**됐다.

**루트 승격** — `/` = `IntroPage`, `/intro`·`/hangul`·`/english` → `/` 301. 위 표 다섯 곳 전부.

**히어로** — 설계엔 「라이브 데모 14 → 1」만 있었는데, 막상 보니 **같은 말을 하는 그림이 넷**
(지도·브릿지·어휘 선반·순환 그림)이었다. 데모를 줄인 것과 **같은 병이 그림으로 다시 자란 것**이다.

| 뺀 것 | 이유 |
|---|---|
| `WordBookMesh`(지도) | 브릿지와 **같은 세 칸** — 개수를 펠트 사진으로 말했을 뿐 |
| `VocabShelf`(어휘 선반) | 같은 문장을 책장으로 세 번째 |
| `LearnReadCycle`(순환) | 셋째 칸 제목이 브릿지 ③ 라벨과 **글자까지 같았다** |
| 「예를 들면…」 잇는 줄 | 이을 상자가 하나가 되면 필요 없다 |

넷 다 **렌더만 뺐고 코드는 남아 있다.**

**절 둘이 사라졌다** — 「왜 탱고북인가」는 히어로 한 줄로(1,546px → 1줄), 「요금」은 FAQ 로.
요금 절은 문단 다섯이 전부 「없습니다」로 끝나는 답이었어서 물음만 붙이니 그대로 항목이 됐다.

| 375px 실측 | 08-18 | 지금 |
|---|---:|---:|
| 페이지 전체 | 12,574px (23.6화면) | **10,988px** |
| 히어로 CTA | 1,817 | **1,465** |
| 「왜 탱고북인가」 절 | 1,546 | 0 |

**가격 정책이 뒤집혔다** — 08-11 의 「가격은 쓰지 않는다」 폐기. FAQ 절 제목 아래 **접히지 않은
한 줄** + FAQ 첫 항목. 금액은 `PLANS` 파생(박으면 오픈 반값 종료일에 랜딩이 거짓말을 한다).
🔴 정가·반값(9,900/79,000)은 **안 쓴다** — 한 답에 숫자가 다섯이면 부모가 자기가 얼마 내는지를
못 고른다. 취소선은 구매 화면 `PlanCard` 몫.

**미결** — 사용자 시안과 일부러 다르게 둔 것 둘(승인 대기): ②칸 설명을 「한글 1,500개 · 영어 필수
어휘」로 안 바꿨다(1,500 은 목표, 실제 낱말 630 — 숫자를 두 곳에서 말하면 하나는 목표 하나는
보유로 갈린다) · ③칸 「햇살 좋은 날…」 문장을 남겼다(노란 하이라이트가 「맞힌 낱말에 색이
들어간다」는 유일한 증거).

---

## ✅ 다국어 (2026-08-21 오후)

루트 승격 직후 소개 페이지를 **ko·en·vi·zh·th** 로 번역했다. 이 문서는 루트를 한국어 광고
도착지로만 설계했는데, 루트가 되고 나니 **도메인을 친 사람 전부의 첫 화면**이라 한국어만으로는
성립하지 않는다(해외 로그인 사용자는 이미 페이월 밖 · `/vi` 마케팅 링크도 이미 나가 있다).

**새 네임스페이스 `landing`** (135키) + 공용 네비 `PublicNav`. i18n 은 `locales/ko/*.json` 글롭에서
ns 를 파생하므로 파일만 놓으면 등록된다 — 설정 수정 없음.

🔴 **번역 대상을 고르는 기준 = 「그게 배우는 대상인가」** (파닉스 다국어와 같은 규칙):

| 그대로 | 이유 |
|---|---|
| 낱말(오리·가구·사과·뿔) · 자모 · 「가구」/「가」 예시 | 아이가 **배우는 한국어** |
| 책 제목(백설공주) | 표지에 한국어로 이미 그려져 있다 |
| `LINES[].match` 의 `'세계 명작'` | **R2 데이터 키** — 번역하면 아무 책도 안 걸린다 |
| SEO `keywords` | 네이버 실측 한국어 — 옮기면 근거가 사라진다 |

🔴 **문장 안 강조는 `<Trans>`** — 조각으로 쪼개면 언어마다 어순이 달라 안 맞는다.

🔴 **`defaultValue: ''` 로 없는 키를 때우지 말 것** — i18next 가 **키 이름을 화면에 찍는다**
(영어 랜딩에 `chain.phonics.n` 이 글자로 나왔다). 빈 키를 두는 것도 `verify-locales` 가 막는다
→ **있는 칸만 그린다고 데이터에 플래그**(`hasCount`·`hasEn`)로 적었다.

~~🔴 **`/vi` 등 언어 링크의 기본 도착지도 `/library` → `/`**(`LangEntry`)~~
🔴 **되돌렸다**(같은 날 사용자) — `/` 로 보내는 근거가 「자기 언어로 왔는데 설명을 못 본다」
였는데, **프리렌더는 한국어 한 벌뿐**이라 `/` 로 보내도 크롤러·첫 페인트는 한국어 HTML 을 받는다.
언어 이득이 없는 대신, `/vi` 가 원래 하던 일(해외 마케팅 → 책장)을 잃는다.
→ 다국어 도착지는 **언어별 프리렌더가 생긴 뒤에** 다시 볼 일이다.

**부수 효과 — 죽은 코드 12개 삭제.** 데이터 모양을 바꾸자 **렌더도 안 되고 컴파일도 안 되는**
컴포넌트가 드러났다(`WordBookMesh`·`VocabShelf`·`LearnReadCycle`·`TryBridge`·`CurriculumUnits`·
`Pict`·`Stat` …). 오전에 「되돌릴 때 쓰게 남겨 둔다」고 했던 것들인데 **빌드가 안 되는 코드는
보존이 아니라 부채**라 지웠다 — **2,100 → 1,405줄(−33%)**.

**검증** — `verify-locales` 4언어 × 14ns 전부 일치 · 클라 테스트 1,032개 통과 ·
5개 언어 실제 렌더에서 키 누출 0 · 가로 넘침 0.

### 🔜 남은 것

- ⚠️ **`/en`·`/vi` URL 은 크롤러에겐 아직 빈 셸**이다 — 클라이언트에서만 언어를 바꾸므로
  **다국어 SEO 는 아직 없다**. 위 「SEO 배선」 표의 다섯 곳을 언어별로 한 번 더 해야 하고,
  블로그·about 처럼 SSR/프리렌더가 붙어야 한다.
- 푸터는 한국어(전자상거래법 신원표시). 링크 라벨은 번역 가능.
- 앱 안 i18n 키는 아직 「어휘 게임」 — 랜딩·네비만 「독후 게임」으로 바뀌었다.

---

## ✅ 첫 화면 무게 (2026-08-21 저녁)

「첫 페이지 로딩 속도 재 보자」로 시작해 **412 → 303KB(gzip, −26%)**. 상세 → memory
`first-screen-weight-2026-08-21`. 이 문서에 남길 것은 **위 「SEO 배선」 표에 여섯 번째가 있었다**는 것.

### 🔴 여섯 번째 — 프리렌더는 굽는 것과 **서빙**이 따로다

`/` 스냅샷은 `dist/home/index.html` 에 구워지는데(`dist/index.html` 은 SPA 폴백이라 못 덮는다)
**매니페스트에서 일부러 빠져 있었다** — `/` 가 `/library` 로 튕기던 시절의 맞는 판단이었다.
루트를 소개 페이지로 올리고도 그걸 안 고쳐서, **첫 화면이 글자 0자인 11.6KB 빈 셸**로 나갔다.
파일은 써지고 빌드는 통과하니 아무것도 시끄럽게 실패하지 않았다.

같은 날 아침 이 문서에 「주소를 옮기면 다섯 곳이 한 벌」이라고 적으면서 그걸 놓쳤다.
**표에 6번을 넣는다:**

| | 곳 | 안 하면 |
|---|---|---|
| 6 | 프리렌더 **매니페스트 + 서버의 파일 자리**(`/` 는 `home/`) | 구워 놓고도 빈 셸을 내보낸다 |

판정 = 배포 후 `curl` 로 받아 **태그를 벗기고 글자 수**를 센다.
⚠️ h1 에 `<span>` 강조가 있으면 **문자열 grep 은 0을 뱉는다**(검사식을 검증 없이 쓰지 말 것).

### 🔴 첫 화면 무게는 「엔트리 크기」가 아니다

판정 = `index.html` 이 부르는 **script + modulepreload + stylesheet 를 전부 gzip 해 더한 값**.
`manualChunks` 로 games·framer 를 갈라 **엔트리를 372→39KB** 로 줄였는데 **첫 화면은 412→427KB
로 늘었다**(갈라낸 청크가 preload 로 따라붙는다). 되돌렸다.

진짜 범인은 **배럴** — 라우터가 정적으로 부르는 `LibraryPage` 의 import 두 줄이 366KB 를 끌어왔다
(`@/features/library` → 죽은 `LibraryBanner` → framer-motion · `@/features/continuous` → 배럴이
재수출하는 연속재생 페이지 셋 → 뷰어·게임 322KB). **첫 화면 진입점 페이지는 실제 모듈 경로로.**

### ⚠️ FCP·LCP 는 못 쟀다

브라우저 도구의 탭이 `document.hidden = true` 라 프레임을 안 그려 paint 엔트리가 안 생긴다.
이 문서의 숫자는 전부 **네트워크·DOM** 기준이다 — 체감은 PageSpeed Insights 로 볼 것.
