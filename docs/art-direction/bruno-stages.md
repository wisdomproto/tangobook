# 브루노와 할아버지 — 무대·사물 시트

> art-director 산출물 (2026-08-16). 시리즈 `bruno-crayonwood` · 25권 250쪽.
> 🔴 앵커 SSOT = `bruno-anchor.md`. 후보 = `_stages.json`(자리 13 · 사물 63항목 / 고유 56) · 대본 = `_scenes.json`

---

## §0. 🔴 매체 번역 한 줄 — 이 매체엔 윤곽선이 없다

앵커: `Direction NEVER changes inside one area - it changes only where one thing ends and another
begins, and THAT CHANGE OF DIRECTION IS THE EDGE.`

**획이 도는 방향이 바뀌는 자리가 곧 가장자리다.** 그러므로 자리 시트가 정하는 것은 「무엇이 있나」가
아니라 🔴 **「어느 면이 어느 방향인가」**다. 방향이 겹치면 두 물건이 **한 덩어리가 된다** — 벽과 바닥이
같은 방향이면 방이 없어진다. → 모든 자리 시트에 `STROKE DIRECTION MAP` 절.

🔴 **둘째 — 하늘·물·얼음·눈은 아예 왁스를 안 얹는다.** 그러니 호숫가·개울가 시트가 정하는 것은
「물을 어떻게 칠하나」가 아니라 **「어디를 안 칠하나」**이고, 물의 형태는 **둘레의 방향들이 만든다**.
21권이 통째로 얼음 이야기라 여기가 제일 위험하다.

⚠️ 셋째 — `DEPTH IS SPACING`. 숲 깊은 데는 줄기가 **좁게 선다**(같은 왁스 무게). 흐리게·푸르게 금지.
⚠️ 넷째 — 🔴 `THE FRAME STAYS WIDE ENOUGH TO HOLD BOTH BEARS FROM THE KNEE UP`(여덟 권이 앞발로
말한다). 자리 시트의 SPOT 에 **앞발만 화면을 채우는 앵글을 만들지 않는다**.

---

## §1. 자리 시트 — 5장 (후보 13에서 접음)

| 시트 | = 후보 | 왜 하나인가 |
|---|---|---|
| `Cabin` | 오두막 부엌 · 오두막 안 · 부엌 식탁 · 식탁 옆 · 창가 · 부엌 창가 · 오두막 창가 · 오두막 안 창가 | 부엌에서 창이 보인다 |
| `CabinOutside` | 마당 · 오두막 마당 · 오두막 앞 · 오두막 문간 · 오두막 옆 텃밭 | |
| `Shed` | 헛간 안 · 헛간 앞마당 | 장작·연장 |
| `Lake` | 호숫가 | 🔴 **21권 얼음** |
| `Wood` | 숲 개울가 · 개울가 · (자작나무 숲) | 01 p1 의 자작나무 한 그루가 고정 부품 |
| ⚠️ `Attic` | 다락 창가 | 🔴 **미결의 답 — 25권** · §3 |
| 🔴 `FeedBoard` | 창밖 모이대 · 창가 모이대 | **신설**(23권 10쪽 중 4쪽) — `Cabin` 은 실내이고 `CabinOutside` 는 마당·문간·텃밭뿐이라 **모이대가 어느 시트에도 없었다.** 창턱 바깥에 달린 판 하나 + 그 아래 눈 딛는 자리. 🔴 23 p4 「파란 장화가 눈을 딛는다」가 **밖**이라 실내로 묶으면 안 된다 |

### §1.1 Cabin — 실제 프롬프트 (가장 먼저)

```
STAGE SHEET - Cabin   (bruno-crayonwood · SCENE token: Cabin · bake FIRST)

The log cabin the two bears live in. Eight of the thirteen named places in this series are inside
it, so this drawing decides the room once - and above all it decides ITS DIRECTIONS.

🔴 STROKE DIRECTION MAP - every area is one field of one crayon laid in ONE direction, and the
  direction changes ONLY at an edge. Fix them here and never change them:
  FLOOR - BARK, strokes running LEFT TO RIGHT along the planks (at most 6 planks).
  BACK WALL - BARK, strokes running VERTICALLY with the logs.
  CEILING BEAMS - OVERLAY, strokes running LEFT TO RIGHT (at most 4 beams).
  TABLE TOP - BARK, strokes running AWAY FROM THE VIEWER, 🔴 which is a third direction and is why
    the table separates from the floor without any outline.
  SHUTTERS AND CLOTH - SPRUCE, strokes running VERTICALLY.
  THE STOVE - OVERLAY, strokes running LEFT TO RIGHT.
  🔴 NO TWO TOUCHING AREAS MAY SHARE A DIRECTION. Check this on the sheet by naming, for each pair
  of neighbours, which way each one runs.

FIXED PARTS: one table with two stools · a low shelf on the back wall · 🔴 ONE WINDOW above the
  table, and THE LIGHT THROUGH IT IS BARE PAPER, unwaxed, the brightest thing in the room · a door
  to the yard · the stove in the corner with at most 5 curls of steam · at most 4 windows in the
  whole cabin, ever.
COVERAGE: wax is at most 80 percent everywhere - the oat-grey grain of the paper shows through
  every single area, and no area is ever solid.

SPOTS:
  A THE WHOLE ROOM from the door, wide: table under the window left, stove right, shelf behind.
  B THE TABLE, medium, 🔴 wide enough to hold two bears from the knee up.
  C THE WINDOW, medium: the bare-paper light and the sill.
  D THE FLOOR, close, high: plank direction and whatever is on it.

PLATE: A, B, C, D once each, plus 🔴 ONE DIRECTION DIAGRAM - the same room drawn as flat areas with
  an arrow in each one showing which way its strokes run.

NOT: no character of any kind, no boot blue anywhere on this sheet (the blue is Nils's boots only),
  no lettering or numerals, no outline of any kind - edges are changes of stroke direction, no
  third crayon, no solid area, no waxed sky, water, ice or snow, no shading, gradient, glow or
  soft edge, nothing darker or paler with distance.
```

### §1.2 Lake — 요약 명세

🔴 **호수는 안 칠한 종이다.** 물가 선은 **땅 쪽 획이 멈추는 자리**이고 물 쪽엔 아무것도 없다.
21권 얼음도 **같은 안 칠한 자리** — 얼음과 물이 같은 종이라 **구멍만이 둘을 가른다**(21권 구멍 6쪽).
구멍 = OVERLAY 한 덩어리, 가장자리는 획 방향이 바뀌는 자리. `SPOTS` = A 물가에서 / B 얼음 위 /
C 구멍 close / D 건너편.

---

## §2. 사물 시트 — 42장 (고유 56에서 노이즈 14 제외)

| 토큰 | 사물 | 권·쪽 | 비고 |
|---|---|---|---|
| `Baskets` | 바구니 | 12(4) · 13(4) · 14(4) | 3권 공유 |
| `Mushrooms` | 버섯 | 14 (8쪽) | 상한 있음 |
| `Firewood` | 장작 · 토막 | 15 (7쪽) | 쌓인 것 / 무너진 것 |
| `Tap` | 꼭지 · 나무 통 | 01 (6쪽) | 자작나무 수액 |
| `Seeds` | 씨앗 · 모이대 | 02(6) · 23(4) | 2권 공유 · 상한 9 |
| `Apples` | 사과 | 13(4) · 17(4) | 2권 공유 |
| `Ladder` | 사다리 | 19(5) · 25(3) | 2권 공유 |
| `Bed` | 침대 · 서까래 | 25 (6쪽) | 다락 |
| … | (나머지 34장) | | |

### §2.1 Tap — 실제 프롬프트

```
PROP SHEET - Tap   (bruno-crayonwood · SCENE token: Tap)

The birch tap and the wooden pail hung under it. One book is ten pages of waiting for a drop.

FORM: THE TAP is a short spout of BARK driven into the trunk, strokes running with the spout.
  THE PAIL is BARK, wide-mouthed, its staves running VERTICALLY, hung on a peg by a rope. 🔴 The
  pail's mouth is a wide ellipse and its INSIDE is OVERLAY with strokes running the other way -
  that direction change is how the mouth reads as open, and there is no outline anywhere.
🔴 THE DROP IS BARE PAPER. Sap, like water, is never waxed. A drop at the spout tip is a small
  unwaxed hole with the surrounding wax stopping at its edge; a drop in the pail is a small unwaxed
  hole in the OVERLAY. 🔴 THE AMOUNT IN THE PAIL IS HOW MUCH OF ITS INSIDE IS LEFT UNWAXED, given
  as a fraction of the pail's depth - draw the ruler: empty · one drop · a finger · half · full.
STATES: 1 the tap in the trunk with the pail hung under it, side on · 2 the spout tip close, one
  drop about to fall · 3 the pail from above, the five levels as five panels.

PLATE: the three states plus the five-level strip.

NOT: no character, no paws, no boot blue, no lettering or numerals, no outline, no third crayon,
  no solid area, no waxed drop or waxed water, no sheen, gradient, glow or soft edge.
```

### §2.2 Firewood — 요약 명세

장작 하나 = BARK 토막 하나, **획이 토막 길이 방향**. 쌓은 더미는 최대 6단, 🔴 **가까운 대여섯만 따로 있고
나머지는 한 면**(RUN 예외). 무너지면 **면이 없어지므로 화면이 비어야 한다** — 어질러 그리면 이 매체의
`FINISHED THINGS PER PAGE = 2` 와 부딪힌다.

---

## §3. 🔴 미결 — 「창가」 4갈래 (6쪽)

> 🔴 **이 §3 은 판정 근거만 적는다. 이름→토큰 변환의 SSOT 는 `_stage-tokens.json` 이다.**
> 표 출력·검사 = `node packages/client/scripts/extract-series-stages.mjs --tokens bruno` (**미매칭 0** 이어야 한다).
> 🔴 **쪽 목록은 폐기했다** — 목록과 변환표를 따로 두니 어긋났다(변환표엔 있는데 목록엔 없는 이름이 나왔다).
> 규칙이 전 이름을 덮으므로 목록이 필요 없고, 「목록 밖이라 안 붙인다」가 생기지 않는다.
> 🔴 **SPOT**: `_stage-tokens.json` 의 `spots` 가 `null` 인 시트는 **시트명만** 붙인다. 값이 있는 시트도
> **A/B/C/D 는 카메라가 정하므로 경로표(§4)에서 정한다** — §3 에서 비워 둔 건 빈칸이 아니라 경로표 몫이다.
> 🔴 **일부 이름은 권을 알아야 정해진다**(`byBook`). 앞 쪽을 읽어야 하는 종류이고, 규칙 파일이 그걸 들고 있다.


| SCENE | 결정 | 종류 |
|---|---|---|
| 부엌 창가 · 오두막 창가 · 오두막 안 창가 | `Cabin` / SPOT C | 같은 창 (좌표) |
| 🔴 **다락 창가** | **`Attic` — 별개 시트** | **다른 곳** (25권. 사다리로 올라간다 — 묶으면 「올라갔다」가 그림에서 사라진다) |

→ 22·23 은 둘 다 부엌 창(모이대가 창밖에 있다) = `Cabin/C`. 다락은 25권이고 이름이 이미 「다락 창가」라
미결이 아니다.

---

## §4. 권별 경로표 — 견본 (01 「한 방울」)

| 쪽 | 자리 | SPOT | 🔴 통 안 높이 | 이어짐 |
|---|---|---|---|---|
| p1 | `Wood` | 자작나무 | 빈 통 | 녹다 남은 눈 = 안 칠한 종이 |
| p2 | `Wood` | 꼭지 close | 방울 하나 맺힘 | 🔴 방울 = 안 칠한 구멍 |
| p3 | `Wood` | 통 위 하이앵글 | **한 방울** | 「바닥이 넓어 더 작아 보인다」 |
| p4~p9 | `Wood` · `Cabin` | | 한 방울 → 손가락 → 절반 | 오가는 사이에 찬다 |
| p10 | `Wood` | 통 위 하이앵글 | **가득** | p3 과 **같은 화면**이라야 찬 것이 보인다 |

🔴 **p3 과 p10 이 같은 각도·같은 거리여야 한다** — §2.1 의 다섯 단계 눈금이 그걸 받는다.
