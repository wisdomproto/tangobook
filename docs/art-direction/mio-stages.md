# 미오네 무지개반 — 무대·사물 시트

> art-director 산출물 (2026-08-16). 시리즈 `mio-cutschool` · 25권 250쪽.
> 🔴 앵커 SSOT = `mio-anchor.md`. 후보 = `_stages.json`(자리 18 · 사물 87항목 / 고유 77) · 대본 = `_scenes.json`

---

## §0. 🔴 매체 번역 한 줄 — 비운 면도 비어 있지 않다

앵커: `Form is made by WHAT THE KNIFE TOOK AWAY - every white line is a gouged channel, never a
drawn line` · `a cleared area is not blank - it carries the parallel furrows of the tool that
cleared it, AT MOST 9 FURROWS PER AREA, all running one way within that area, and the direction
changes only where one thing ends and another begins.`

🔴 **흰 것은 그린 선이 아니라 파낸 골이고, 비운 면에는 그 골이 최대 아홉 줄 남는다.** 그러므로 자리 시트가
정하는 것은 **「이 면의 골이 어느 방향으로 몇 줄인가」**다. 방향이 바뀌는 자리가 곧 물건이 갈리는 자리라
윤곽선을 그릴 필요가 없고, 🔴 **방향을 안 정해 두면 교실 바닥과 벽이 한 덩어리가 된다.**

🔴 **둘째 — 한 스프레드에 칼이 한 번 미끄러지고 그걸 남긴다**(`THE KNIFE SLIPS ONCE PER SPREAD AND
THAT SLIP IS LEFT IN`). 시트에도 미끄러진 자국을 하나씩 둔다. 안 두면 컷이 반듯해지고, 반듯한 리노컷은
벡터 아트로 보인다.

⚠️ **딩딩(전지)과의 판정축** — 둘 다 「깎아 낸다」라 위험하다. 딩딩은 **종이를 오려 얹고 모든 검정이
이어져야** 하며 흰 것은 **밑장**이다. 미오는 **판을 파서 찍고** 흰 것은 **파낸 골**이라 그 안에 결이 남는다.
🔴 **판정 = 흰 면에 결이 있는가**(미오 있음 / 딩딩 없음).
⚠️ 셋째 — 🔴 `THE CLASS IS FIVE CHILDREN AND ONE TEACHER AND NOBODY ELSE`. 자리가 비면 **빈 채로 그린다**.

---

## §1. 자리 시트 — 5장 (후보 18에서 접음)

| 시트 | = 후보 | 왜 하나인가 |
|---|---|---|
| `Classroom` | 교실 · 무지개반 교실 · 교실 창가 · 책상 앞 · 책상 옆 · 그림 책상 옆 · 창턱 앞 | 창가·책상이 한 방 안 |
| `Kindergarten` | 마당 · 유치원 마당 · 마당 가운데 · 나무 밑 · 문간과 마당 · 교실 문과 마당 · 통 옆 · 통 앞 · 문 안팎 · 창고 문 안팎 | 🔴 **한 마당** — 나무·통·창고가 다 그 안 |
| `River` | 강가 · 마을 강가 · 물가 · 물 위 · 강가 길 | |
| `Shingle` | 자갈밭 · 강가 자갈밭 · 물가 자갈밭 | 🔴 강가의 **다른 바닥** |
| `TreeHouse` | (나무집) | 18권 · 사다리 |

### §1.1 Classroom — 실제 프롬프트 (가장 먼저)

```
STAGE SHEET - Classroom   (mio-cutschool · SCENE token: Classroom · bake FIRST)

The Rainbow class room. Seven of the eighteen named places in this series are inside it, so this
drawing decides it once - and above all it decides ITS FURROWS.

🔴 THE FURROW MAP - a cleared area is not blank; it carries the parallel gouges of the tool, AT
  MOST 9 PER AREA, all running one way inside that area. The direction changes only at an edge, and
  THAT is what separates two things - never a drawn line. Fix them here:
  FLOOR - SOOT cleared, furrows running LEFT TO RIGHT.
  BACK WALL - SOOT cleared, furrows running VERTICALLY.
  CEILING - cleared, furrows running AWAY FROM THE VIEWER.
  DESK TOPS - cleared, furrows running AWAY FROM THE VIEWER, 🔴 which is why a desk lifts off the
    floor with no outline at all.
  THE PLAY-HOUSE ROOF - RIVER, furrows with the slope.
  🔴 NO TWO TOUCHING AREAS SHARE A FURROW DIRECTION. Name each neighbouring pair on the sheet.

FIXED PARTS: FIVE low desks and five chairs, no more · a low shelf of jars, at most 6 of one shape
  · COAT HOOKS on the near wall, at most 6 · 🔴 SIX WINDOWS as rectangles with 0 frames, and THE
  WINDOW LIGHT IS UNPRINTED PAPER, the brightest thing in the room · the store cupboard at the
  back, its inside OVERLAP · a block corner with a mat · the teacher's tall stool.
EVERY EDGE IS A CUT EDGE - slightly ragged, never mechanically smooth. 🔴 ONE KNIFE SLIP per panel,
  left in.

SPOTS:
  A THE WHOLE ROOM from the door, wide: desks left, windows along the back, cupboard right.
  B A DESK, medium, from a child's height.
  C THE WINDOW SILL, close: unprinted light and what sits on it.
  D THE BLOCK CORNER, medium high: the mat and the floor furrows.

PLATE: A, B, C, D once each, plus 🔴 A FURROW DIAGRAM - the room as flat areas with an arrow in
  each showing which way its gouges run - plus one panel of a knife slip done correctly.

NOT: no character of any kind, no accent purple anywhere on this sheet, no lettering, numerals or
  wall charts, no drawn white line - every white is a gouged channel, no third block, no smooth
  mechanical edge, no cleared area left blank without furrows, no shading, gradient, glow, cast
  shadow or soft edge.
```

### §1.2 Kindergarten · River — 요약 명세

`Kindergarten` = 마당 바닥(SOOT 고랑 가로) · 🔴 **큰 나무 하나**(밑동 OVERLAP) · 창고(안쪽 OVERLAP) ·
그네 · 물통 · 문간. `River` = 강 = RIVER 한 면, 🔴 **둑 밑은 OVERLAP** · 오리 최대 5 · 물결 0.
`Shingle` = 🔴 **자갈 = 고랑이 아니라 파낸 점**(14·25권), 강가와 바닥이 달라 별개 시트.

---

## §2. 사물 시트 — 58장 (고유 77에서 노이즈 19 제외 · 🔴 15시리즈 중 최다)

| 토큰 | 사물 | 권·쪽 | 비고 |
|---|---|---|---|
| `PaperChain` | 고리 · 사슬 · 빨강 노랑 초록 | 19 (5·5·10·10·9) · 22(6) | 🔴 **상한 14 · 색 셋인데 이 매체는 잉크 둘** · §2.1 |
| `Blocks` | 블록 | 01 (5쪽) | 상한 12 |
| `Paper` | 종이 | 05(3) · 09(5) · 11(6) · 25(3) | 4권 공유 |
| `Pots` | 화분 | 04 (9쪽) | |
| `Bag` | 가방 · 도토리 | 10 (7·4·4) | |
| `Ladder` | 사다리 · 나무집 | 18 (7·5) | |
| `Broom` | 빗자루 · 낙엽 | 21 (7·4) | 상한 11 |
| `Shoes` | 신발 | 15 (6쪽) | 짝이 안 맞는 것 |
| … | (나머지 50장) | | |

### §2.1 PaperChain — 실제 프롬프트 (🔴 색 셋을 잉크 둘로)

```
PROP SHEET - PaperChain   (mio-cutschool · SCENE token: PaperChain)

The paper chain the class makes. One book counts its links for ten pages, and 🔴 THE SCRIPT NAMES
THREE COLOURS (red, yellow, green) WHILE THIS MEDIUM HAS TWO INKS AND NEITHER IS ANY OF THEM.

🔴 THE ANSWER IS NOT COLOUR, IT IS THE CUT. The three kinds of link are told apart by what the
  knife did, and this never changes:
  KIND 1 - SOOT, solid, 0 furrows inside.
  KIND 2 - cleared, furrows running ALONG the link, at most 4.
  KIND 3 - RIVER, solid, 0 furrows.
  A child can then say "the pattern goes one-two-three, one-two-three" without any colour at all,
  and the pattern is countable in print and in a photocopy.
FORM: each link is a flat ring, cut edge slightly ragged, and 🔴 AT MOST 14 LINKS ever appear
  clearly; a longer chain carries on as ONE joined shape with 0 interior detail (a RUN is exempt).
  Where two links interlock, the crossing is OVERLAP.
STATES:
  1 THREE LINKS, one of each kind, large, so the three cuts are unmistakable.
  2 A RUN OF NINE in pattern order, hanging in a shallow curve.
  3 A LONG CHAIN across the room - the near fourteen separate, the rest one joined shape.
  4 A BROKEN CHAIN, the two ends apart, 🔴 and the pattern must still be readable at both ends so
    a reader can see which kind is missing.

PLATE: the four states, plus one panel of the three cuts side by side at large size.

NOT: no character, no hands, no accent purple, no lettering or numerals, 🔴 no red, yellow or green
  anywhere - the three kinds are cuts, not colours / no drawn white line, no third block, no
  smooth mechanical edge, no shading, gradient, glow or soft edge.
```

---

## §3. 🔴 미결 — 「마당」 4갈래(29쪽) · 「자갈밭」 3갈래(6쪽) · 「강가 길」 2갈래(3쪽)

> 🔴 **이 §3 은 판정 근거만 적는다. 이름→토큰 변환의 SSOT 는 `_stage-tokens.json` 이다.**
> 표 출력·검사 = `node packages/client/scripts/extract-series-stages.mjs --tokens mio` (**미매칭 0** 이어야 한다).
> 🔴 **쪽 목록은 폐기했다** — 목록과 변환표를 따로 두니 어긋났다(변환표엔 있는데 목록엔 없는 이름이 나왔다).
> 규칙이 전 이름을 덮으므로 목록이 필요 없고, 「목록 밖이라 안 붙인다」가 생기지 않는다.
> 🔴 **SPOT**: `_stage-tokens.json` 의 `spots` 가 `null` 인 시트는 **시트명만** 붙인다. 값이 있는 시트도
> **A/B/C/D 는 카메라가 정하므로 경로표(§4)에서 정한다** — §3 에서 비워 둔 건 빈칸이 아니라 경로표 몫이다.
> 🔴 **일부 이름은 권을 알아야 정해진다**(`byBook`). 앞 쪽을 읽어야 하는 종류이고, 규칙 파일이 그걸 들고 있다.


| SCENE | 결정 | 종류 |
|---|---|---|
| 유치원 마당 · 유치원 문간과 마당 · 교실 문과 마당 · 나무 밑 마당 | `Kindergarten` (+ SPOT) | **좌표** — 한 마당 |
| 강가 자갈밭 · 물가 자갈밭 · 강가를 따라 걷는 자갈밭 | `Shingle` (+ SPOT) | 좌표 |
| 유치원 문 앞과 강가 길 · 마을 강가 길 | `River` / 길 | 좌표 |

⚠️ **「나무 밑」이 두 곳에 있다** — `Kindergarten` 의 큰 나무(02·16권)와 `TreeHouse` 의 나무(18권).
묶으면 18권의 「사다리를 타고 올라갔다」가 마당 나무 위가 되어 버린다 → 별개.


---

## §4. 권별 경로표 → `mio-routes.md`

🔴 **권별 경로표는 `mio-routes.md` 로 옮겼다**(25권 250쪽 전부). 여기 있던 19권 견본도 그 파일의 19권 자리에 있다.
같은 표를 두 곳에 두면 갈라진다 — §3 의 「쪽 목록」이 그렇게 됐고 지우는 걸로 끝냈다.

### §4.1 19권의 색 — 판정 절차만 남긴다

🔴 **이 권은 「색 셋」으로 쓰여 있는데 매체엔 잉크 둘뿐이고, 그 둘 중 어느 것도 빨강·노랑·초록이 아니다.**
SCENE 은 **열 쪽 전부**에 「색은 빨강·노랑·초록」이라 못 박아 놨다(그리고 「보라는 절대 안 쓴다」까지 —
악센트를 피하려고 일부러 그랬다).

**대본은 안 고친다. 확인했다** — 본문(낭독 텍스트)은 「**색종이**」라고만 하고 세 색 이름을 **한 번도
안 부른다**(`15-25.md` 19권 전문 확인). 색은 SCENE 에만 있으므로 **§2.1 처럼 칼자국 셋으로 옮기면**
낭독과 어긋날 데가 없고, 무늬는 그대로 세어지며 흑백 축소에서도 살아남는다.

🔴 **판정 절차를 남긴다** — 대본↔앵커가 색으로 부딪히면 **먼저 본문이 그 색을 부르는지 본다.**
안 부르면 그림 처방, 부르면 작가에게 넘긴다. SCENE 만 보고 「대본을 고쳐야 한다」고 하면 안 고쳐도 될 것을
고치게 된다.
