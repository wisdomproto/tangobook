# TangoBook 공유 메모리

2026-09-21에 Claude 메모리와 현재 코드/이번 세션 결과를 대조한 요약이다.
과거 수치는 당시 관찰값이며, 현재 상태는 다시 확인한다. 상세 원본은 [목록](legacy-memory-index.md)에서 찾는다.

## 사용자 작업 방식

- 한국어 존댓말. 제품 카피의 아이/부모 대상 말투와 개발 대화 말투는 별개다.
- 사용자는 매번 설명을 반복하지 않고 같은 동료와 이어서 대화하는 경험을 원한다(2026-09-21). [기능별 작업 지도](../work/README.md)의 기억과 진행 기록을 먼저 읽는다. 기능이 명확하면 바로 이어가고, 막연한 시작 요청에만 실제 진행 작업 선택지를 제공한다.
- 기존 자산과 구현부터 찾고, 같은 기능의 사본을 늘리지 않는다.
- 필요한 로컬 작업은 이어서 완료한다. 이미 허락받은 일을 단계마다 다시 묻지 않는다.
- 로컬 커밋은 하되 main push는 해당 작업에 대한 사용자 요청이 있을 때만. Railway 자동 배포 때문에 시점을 사용자가 정한다.
- 새로 알게 된 사실은 즉시 관련 지침에 이유·실제 증상·검증법과 함께 남긴다. 반증된 규칙은 왜 틀렸는지 기록한다.

출처: `user-honorifics-2026-07-26.md`, `no-push-without-ask-2026-09-07.md`, `auto-update-agents-2026-08-10.md`, 기존 MEMORY.md.

## Git와 운영 환경

- `git add -A` 때문에 다른 작업의 파일 1,033개를 stage/커밋한 과거 사고가 있다. 파일별 stage와 실제 Git root/branch 확인이 필요하다.
- 워크트리가 사라져도 상위 저장소에서 Git 명령이 성공할 수 있다. `git status` 성공만으로 올바른 저장소라고 판단하지 않는다.
- 로컬 main과 현재 루트는 원격 main보다 오래됐을 수 있다. 보드 작업은 2026-09-21 `board-white-plate` worktree에서 수행했다.
- 2026-09-17 정리에서 사용하지 않는 로컬 브랜치 24개를 `refs/archive/codex-cleanup-20260917/`에 보관했다. 복구 목록은 공통 Git 디렉터리의 `codex-branch-cleanup-20260917/`에 있다. 이후 상태는 재확인한다.
- 2026-09-21 루트를 main으로 전환하고, 최근 30일간 Git 사용 기록이 없고 checkout되지 않은 로컬 브랜치 35개를 보관 후 삭제했다(51개 남음). 복구 참조는 `refs/archive/codex-cleanup-20260921/`, 기록은 공통 Git 디렉터리의 `codex-main-migration-20260921/REPORT.md`다. 루트의 이전 미커밋 변경과 미추적 파일 1,032개는 같은 참조 아래 `root-wip`에 보관했으며 main에 덮어씌우지 않았다. 옛 main 작업 폴더는 기존 파일을 유지한 채 detached 상태로 남겼다.
- 로컬 서버에서 `DISABLE_PUBLISH_SCHEDULER=1` 없이 운영 자격증명을 읽으면 예약 게시가 실제 실행될 수 있다. 코드의 옵션 처리와 시작 로그를 확인한다.
- R2/Supabase는 로컬 개발 서버에서도 운영 데이터를 가리킬 수 있다. 테스트라고 임의 저장·삭제·시딩하지 않는다.

출처: `worktree-gone-git-add-all-2026-08-21.md`, 루트 CLAUDE.md의 서버 gotcha, 이번 세션.

## 현재 모델과 제품 결정

- 2026-09-21 [모야 전체 표정 프롬프트 수정](../work/content/tasks/20260921-moya-expression-prompts.md): 진단 후 사용자 요청으로 공통 앵커·시트 규격·25권 250컷을 수정했다. 외형 유지와 표정 고정을 구분하며 눈/입/귀 변화를 허용한다. 로컬 반영, 이미지 재생성·push 없음. 아래 진단의 '미요청'은 진단 당시 상태다.

- 2026-09-21 [모야네 물웅덩이 표정 진단](../work/content/tasks/20260921-moya-expression-audit.md): 등록 190쪽 검토. 공통 앵커의 눈 고정·목 중심 감정 지시와 본문 감정의 충돌을 확인했다. 이미지/프롬프트 수정은 아직 요청되지 않았다.

- 2026-09-21 [창작동화 11·12·13·15 캐릭터 비교](../work/content/tasks/20260921-series-character-comparison.md): 표본 이미지와 프롬프트 대조만 수행. 15번은 본문끼리도 외형이 달라 기준 그림 확정이 필요하며, 12번은 시트에 없는 성인 머리장식이 본문에는 있다. 수정·push 요청으로 확대하지 않는다.

- **한 책 = 한 그림체·한 레벨**(2026-09-14). 그림체 간 연결은 책 그룹으로 표현한다. 과거 `styleAssets`, `defaultStyle`, 레벨 사본 설계는 현재 설계가 아니다.
- id 변경은 학습 이벤트·마케팅 영상·게임 자산 링크에도 영향을 준다. 호출부/소비자를 확인하고 기존 aliases를 보존한다.
- **베타 개방은 `BETA_OPEN` 덮개**다. 이전 유료화 플래그들을 각각 뒤집지 않는다. 사용자가 베타 종료를 정한다. 실제 값은 `packages/client/src/features/access/config.ts`에서 확인한다.
- 게이팅 검증은 라우트뿐 아니라 버튼과 단원 진입도 포함한다. Supabase 미설정으로 원래 열린 환경에서는 개방 플래그를 검증할 수 없다.
- 파닉스의 콘텐츠 언어와 UI 언어는 다르다. 한글/영어 글자는 학습 언어, 안내·칭찬은 사용자 UI 언어다.
- 영어 블록게임은 Book 2~5에 있고 Book 1은 제외한다. 전 권 재도입과 Book 1 제외는 별개 결정이다.
- 전략의 원본은 `docs/STRATEGY.md`. 기존 콘텐츠 수·검색량을 새로 추정하지 않고 관련 현황/API/기록을 먼저 확인한다.

출처: `one-book-one-style-2026-09-14.md`, `beta-open-flag-2026-09-04.md`, `in-app-board-camera-2026-09-17.md`, 기능별 CLAUDE.md.

## 검증에서 반복된 함정

- 비교 대상 버전·입력·실행 경로를 먼저 기록한다. 다른 코드/해상도/상태를 재고 같은 제품 결과로 보고하지 않는다.
- 화면의 `0`은 실제 측정 0인지 미갱신 값인지 대입부를 확인한다. 오래된 보드 디버그의 `점 0`이 대표 사례다.
- 서버 TS 빌드가 통과해도 shared 상대 import의 `.js` 누락은 운영 Node 실행에서 실패한다.
- jsdom 미지원 API mock은 `packages/client/src/test/setup.ts` 한 곳에서 관리한다. 실패를 제품 결함/변경된 사양/테스트 환경으로 나눈다.
- iOS는 실제 재생할 Audio 요소를 사용자 제스처에서 해금해야 한다. 기존 `lib/audio-unlock.ts`와 요소 재사용을 유지한다. 로딩이 끝났다는 사실은 재생 허가의 증거가 아니다.
- 첫 화면 성능은 JS entry 하나가 아니라 HTML이 요청하는 preload·CSS 등을 함께 측정한다. 엔트리 크기만 줄인 청크 분할이 전체 전송량을 늘린 전례가 있다.

출처: `measuring-discipline-2026-08-30.md`, `one-book-one-style-2026-09-14.md`, 루트 CLAUDE.md와 vite.config.ts.

## 최근 작업: 흰색 판 인식

- `1822d143`를 원격 main에 push했다. 변경은 `tango-reco.js`의 중첩 카드 윤곽 처리 한 건이다.
- 09:48 프레임: 7개/`댲굽` → 8개/`댲굽바`. 09:44 프레임: 6개/`댜굽`으로 미해결.
- 기존 한글 11장·영어 2장의 낱말/검출 개수는 불변이다. 기존 실패까지 정상화됐다는 뜻은 아니다.
- 정지 이미지 재생 검증만 수행했다. 배포 완료와 실기기 연속 인식은 확인하지 않았다.
- 자세한 재현 절차·입력 키·결과는 [board-camera.md](board-camera.md).

## 갱신 방식

- 2026-09-21 [피포 외형 기준 확정과 프롬프트 수정](../work/content/tasks/20260921-pipo-character-prompt.md): 본문 삽화의 흰 몸·긴 귀 기준. 로컬 main 반영, 미푸시. 기존 회색 시트 이미지 자체는 미교체.
- 2026-09-21 [피포 캐릭터 레퍼런스 진단](../work/content/tasks/20260921-pipo-reference-audit.md): 시트/기존 삽화 외형 불일치와 복사·배치 경로의 참조 전달 차이를 확인했다. 생성 코드·운영 데이터는 변경하지 않았다.
- 네이버 블로그 재개 현황과 발행기 위치는 [마케팅 기억](../work/marketing/MEMORY.md) 및 연결된 2026-09-21 작업 기록을 확인한다.

- 2026-09-21 [로컬 저장공간 진단과 미완료 정리](../work/platform/tasks/20260921-local-storage-cleanup.md): 오래된 worktree 의존성이 주요 용량 원인이다. 자동 승인 검토가 삭제를 차단했으므로 정리 완료로 취급하지 않는다.

이 파일에는 세션을 넘어 필요한 결정과 링크만 둔다. 상세는 해당 주제 문서에 날짜·대상 커밋·입력·측정법·한계와 함께 적는다.
기존 300개 메모리를 현재 규칙으로 일괄 승격하지 않는다. 특히 3월 인덱스와 9월 모델 개편 전 기록은 역사 자료다.
